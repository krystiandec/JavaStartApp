<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.javastartapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JavaStartApp">


        <activity
            android:name=".activity.InputActivity"
            android:label="Łączenie layoutu z Activity">
        </activity>

        <activity
            android:name=".activity.SecondActivity_Intent"
            android:label="Intents">
        </activity>

        <activity
            android:name=".activity.RecyclerViewActivity"
            android:label="Przykład użycia RecycklerView">
        </activity>

        <activity
            android:name=".presenter.RecyclerViewTwoColoursBackground"
            android:label="Test kolorów">
        </activity>

        <activity
            android:name=".activity.ActivityLayoutDependingOnOrientation"
            android:label="Test kolorów">
        </activity>
        <!--fragmenty-->
        <activity android:name=".activity.AnimalFragmentActivity">

        </activity>
        <activity android:name=".activity.AnimalFragmentListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>